# SPA fallback for React Router
/*    /index.html   200

# Force HTTPS (handled by Netlify by default, but can be explicit)
http://aqua-power.nl/*    https://aqua-power.nl/:splat    301!
http://www.aqua-power.nl/*    https://aqua-power.nl/:splat    301!

# Redirect www to non-www (handled by domain settings, but backup)
https://www.aqua-power.nl/*    https://aqua-power.nl/:splat    301! 