# Static files should be served directly - no redirects needed for existing files
# This is handled automatically by Netlify

# SPA fallback for React Router (only for non-existing files)
/*    /index.html   200 